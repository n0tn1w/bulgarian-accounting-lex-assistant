"""Local-only tooling to measure and improve preprocessing on labeled real documents.

Everything here reads a gitignored dataset directory (PREPROCESSING_DATA_DIR); no real
documents, caches or trained models are ever committed. The learned layers are assist-only
— amounts and VAT stay rule-computed and auditable.
"""
