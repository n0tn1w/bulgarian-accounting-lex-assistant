# Full backend dependencies for the running application. Everything the fullstack
# app needs in production lives here, so a single `pip install -r requirements.txt`
# (locally or in the Docker image) reproduces the working environment.

# Web framework + server
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9

# IR / comparison engine
scikit-learn>=1.4
numpy>=1.26
scipy>=1.12

# Safe XML parsing (XXE protection)
defusedxml>=0.7.1

# Persistence + multi-tenancy + vectors
SQLAlchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.3

# Auth
bcrypt>=4.1
PyJWT>=2.8

# Provider-agnostic LLM access (Ollama / Claude / OpenAI / …)
litellm>=1.40

# OCR. Also needs the system Tesseract + Poppler packages (installed in the Dockerfile;
# locally: brew install tesseract tesseract-lang poppler
#          apt-get install tesseract-ocr tesseract-ocr-bul poppler-utils)
pytesseract>=0.3.10
pdf2image>=1.16.3
pillow>=10.3
opencv-python-headless>=4.9

# Commercial-register lookup by EIK (scrapes web.company.guru)
requests>=2.31
beautifulsoup4>=4.12

# Laws RAG (lex) engine. Heavy: pulls torch via sentence-transformers and the
# bge-m3 + reranker models (~2GB, downloaded on first use / first ingest).
lxml>=5.0
chromadb>=0.5
sentence-transformers>=3.0,<4.0
rank-bm25>=0.2.2
simplemma>=1.1
tqdm>=4.66
